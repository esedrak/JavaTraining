package com.javatraining.basics.interfaces;

/**
 * A simple functional interface demonstrating that a class
 * can implement multiple interfaces (Java's form of multiple inheritance of type).
 */
public interface Printable {
    void print();
}
